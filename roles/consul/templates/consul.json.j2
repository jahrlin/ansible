{
  "start_join": [{% for host in consul_servers | difference(ansible_all_ipv4_addresses) %}"{{host}}"{% if not loop.last %}, {% endif %}{% endfor %}],
  "bootstrap": {{consul_node_role | default('false')}},
  "client_addr": "0.0.0.0"
}

